[phases.setup]
nixPkgs = ["nodejs", "corepack"]

[phases.install]
cmds = [
  "corepack enable",
  "cd artifacts/api-server && pnpm install --no-frozen-lockfile"
]

[phases.build]
cmds = [
  "cd artifacts/api-server && pnpm run build"
]

[start]
cmd = "node artifacts/api-server/dist/index.mjs"